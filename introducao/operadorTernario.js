const idadeUsuario = 19
const idadeMinima = 18

/**
 * primeiro a operacao de comparacao depois veio a ? esse sinal significa
 * o que vai acontec er depois, seria os treços de codigo,
 * depois a primeira condição, caso verdadeiro separado por dois pontos a 
 * condição falsa
 */
console.log(idadeUsuario >= idadeMinima ? "cerveja" : "suco")