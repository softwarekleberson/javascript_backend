const nomes = ['julia Pascal', 'HECTOR Hernandes', 'joão paulo']

const nomesAtualizados = nomes.map(nome => nome.toUpperCase())
console.log(nomesAtualizados)

/**
 * metodo toUpperCase transforma tudo em maiscula
 */